<div class="mx-auto sm:max-w-7xl xl:max-w-[100rem] px-3 sm:px-6 lg:px-8">
    {{> headerSection}}
    {{> qrCode}}
    <ul role="list" class="grid grid-cols-1 gap-x-4 gap-y-14 sm:grid-cols-2 md:grid-cols-3 sm:gap-x-14 xl:grid-cols-4 2xl:grid-cols-5">
        {{#each files}}
            {{> photoItem}}
        {{/each}}
    </ul>
</div>
